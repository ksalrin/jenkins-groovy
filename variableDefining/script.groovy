

def objects = [:]

objects['name'] = 'Farkhod'

def isFarkhod = (objects['name'] == 'Farkhod') ? true : false
def isAbdul = (objects['name'] == 'Abdul') ? true : false

println(isFarkhod)
println(isAbdul)
